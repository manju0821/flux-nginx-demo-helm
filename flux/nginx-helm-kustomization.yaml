apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: nginx-helm
  namespace: flux-system
spec:
  interval: 5m
  path: ./flux
  prune: true
  sourceRef:
    kind: GitRepository
    name: nginx-helm-repository
